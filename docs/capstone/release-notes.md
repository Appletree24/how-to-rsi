# RepoOps 实战主线交付记录

基线：`7122d58c2cdcd437499f8abf01c8e243c2e6ab2c`；日期：2026-09-19。

## 为什么增加这一主线

原研究章节提供概念与方法，合成评测实验验证统计规则，但缺少贯穿项目的工程任务、持续交付和求职反馈。本次用一个仓库运维 Agent 串起数据、工具、检索、评分、调度、可观测性、统计、隔离执行、搜索与元改进。

## 已写入

- P0–P11 共 12 篇课程正文，16 周建议节奏，每章有实现/验收与就业证据。
- 首页入口、模块导航、正文搜索、无 JS 目录，与既有学习进度兼容。
- Python 标准库入门实现：严格动作协议、只读检索与读取、两种规则策略、独立 grader、SQLite 原子 trial 与续跑、JSON 报告。
- 本机 Ollama 适配器与 mock HTTP 契约测试；12 个原创公开教学任务，按 6 个服务家族拆分。
- 实现工作簿：检索合同、grader mutant、SQL/竞态、故障/成本、统计、Harbor 任务规格、GEPA 接口和元实验记录。
- 3 份招聘原文、2 份有明确可信度限制的公开面试资料、4 个开源项目固定源码入口；来源总数从 23 增至 36。
- 求职岗位矩阵、作品证据与反馈台账模板。

## 验证（2026-09-19 的历史命令）

```sh
python scripts/build_research.py
python scripts/check_content.py
python -m unittest discover -s tests -v
python -m projects.repoops.runner demo
node --check assets/js/research-data.js
node --check assets/js/app.js
node --check assets/js/research.js
node --check scripts/smoke_browser.cjs
git diff --check
```

本地结果：内容生成一致；46 个导航条目、36 项来源；25 项 Python 测试通过。离线 demo 完成 6 个开发任务的两组运行并产生 SQLite/JSON 结果。新增项目命令已经加入 CI。

浏览器 smoke 脚本扩展了新章节、全文搜索和无 JS 检查，但本环境没有 Chromium；上一轮下载浏览器超时，本轮没有重复下载。因此不将浏览器视觉与交互测试记为通过。

前端已于后续迁移到 Vite + TypeScript；以上 JS 文件路径是历史记录。当前使用 `npm run build` 与 `npm run test:browser`，部署步骤见仓库根目录的 README。

## 限制

- 规则策略与人工夹具的 0/1 成绩只验证流水线，不是模型或搜索能力提升。
- 真实模型权重未运行；Ollama 仅验证 HTTP 契约。模型修订由运行者声明，需要实际核对。
- SQLite runner 是串行教学实现，崩溃时未提交任务会重新调用，外部费用可能重复。
- Go 服务、混合检索、自然语言 judge、Harbor/GEPA 接入和元改进实验是完整课程中的进阶作业，未作为完成的系统提交。
- 上游 commit 只锁源码审阅对象，不是经过运行验证的依赖锁；没有发布网站或合并主分支。

每个进一步的能力主张都应附运行命令、环境、数据与结果；课程和实现状态必须一起维护。

## 整站技术答辩重构（2026-09-20）

- 全部46个导航页面及隐藏笔记页改为问题、机制、取舍、反例与证据驱动；保留实验、公式、交互容器和书签ID。
- 首页、任务书和P0统一知识助手、仓库修复、评测平台三条路线；P11提供45–60分钟追问树与基于可观察行为的评审。
- 16道测验改为跨模块场景判断；结果不再宣称掌握DSH或项目能力，不比较不同题版的历史分数。
- 所有正文统一迁入 `content/chapters/`，生成器同时同步全部正文与无JS目录。
- P3/P5故障实验的召回/上下文、权限不可观测、租约边界、冲突提交、取消顺序和独立费用进入自动测试。
- 新增5项官方文档/固定版本来源登记，复核Harbor产物传递与GEPA接口边界；HF图不再暗示两条攻击线是先后跳板，评分图修正禁用动作取反。
- 实际验证：37项Python测试通过；规则runner、检索、租约、改进器和评测CLI运行通过；缺行输入退出2，步骤耗尽报告保留失败与不完整矩阵。
- 内容、配图、类型和构建检查通过；浏览器smoke覆盖46路由，另逐页检查47页面在390/1440宽度无页面级横向溢出，并完成16题到P11的实际操作。

未运行真实模型、DSH、Harbor或GEPA生产系统；新增工程推演与验收设计不等于这些系统已经实现。此前交付记录保留为历史，不代表当前命令或范围。

## Harness 设计融入

- 首页增加贯穿三条项目路线的 Harness 阅读路径；职责章与配图区分模型策略、运行控制和独立验收，加入工作流、单 Agent、多 Agent 的选型取舍。
- 架构、循环、会话章增加上下文编译与信任分层、32k 窗口裁剪算例、并行读与串行写、根预算、取消收尾、未知副作用查证和 checkpoint 发布协议。
- P2 与项目任务书补充模块责任、完整执行时序和设计交付物；测试章补充脚本化故障注入；P7 增加固定模型的提示词 / 工具接口 / 上下文策略消融矩阵。
- 本轮新增机制均为明确标注的设计与待执行验收，不表示 RepoOps 或 DSH 已实现这些能力；未运行新模型实验，也没有编造效果数据。
- 实际验证：内容与配图检查、类型检查、生产构建通过；浏览器 smoke 覆盖46路由；本轮涉及9个页面在390与1440宽度均无页面级横向溢出，并检查了概览图和移动端消融表的实际渲染。

## 公开文章编辑

- 首页与全部章节改为面向公开读者的技术文章；面试讨论集中在 P11，其他章节按各自主题组织机制、推导与实验。
- 删除定制培训、编辑汇报和重复追问模板；保留公式、代码、运行命令、来源与实现范围。同步导航标题、测验解析、图中文字及公开配套说明。
- 贡献指南明确文章的叙述要求，编辑过程留在维护记录中。
- 内容与配图检查、类型检查、生产构建及46路由浏览器 smoke 通过；47个页面完成390/1440宽度检查，16道测验完整作答并进入项目面试页。
