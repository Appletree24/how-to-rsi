# 一个 Agent 到底在做什么

谈"AI 改自己"之前，得先讲清楚一件事：一个 Agent 干活的时候，到底发生了什么。

简单说，模型只负责一件事：根据已有的上下文，选下一步做什么。剩下全是 Harness 的事——把哪些材料塞进上下文、哪些工具允许它调、调用结果怎么写回来、出错了怎么处理。

这一步看着小，边界却很清楚：模型提出动作，程序决定这个动作能不能执行、执行后剩下什么。后面所有的评测和改进，都建立在这个边界上。
